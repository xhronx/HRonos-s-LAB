#! /bin/bash
# Просто фильтрик, а может не ожин чтобы не забывать удобные способы мониторинга

sudo dmesg 

# udevadm info
# This command runs as an query within the database and give that list outs the info as the output.

# udevadm monitor
